server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    cloud:
      config:
        server:
          git:
            uri: https://github.com/GamithaGimhana/Automated-Greenhouse-Management-System
            search-paths: config-files