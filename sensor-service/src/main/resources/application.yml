server:
  port: 8082

spring:
  config:
    import: optional:configserver:http://localhost:8888

  application:
    name: sensor-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/